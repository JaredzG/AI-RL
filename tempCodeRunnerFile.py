    # agent.set_possible_cells()
    # cells = agent.possible_cells
    # print("cells", cells)
    # q_vals = q_table.find_q_vals(cur_state.representation['male_position']['cell_type'], agent.possible_actions) if agent.type == 'm' else q_table(cur_state.representation['female_position']['cell_type'], agent.possible_actions)
    # print("q_vals", q_vals)
    # action_chosen = policy.pRandom(agent.carrying, agent.possible_cells, )